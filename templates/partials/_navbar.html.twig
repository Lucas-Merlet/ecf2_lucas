<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_intern_index') }}">
            AFPA · Gestion des absences
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#mainNav" aria-controls="mainNav"
                aria-expanded="false" aria-label="Menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNav">
                        <ul class="navbar-nav ms-auto align-items-lg-center gap-2">
                {# --- Liens visibles par TOUS (admin + visiteur) --- #}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_intern_index') }}">Trombinoscope</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_stats') }}">Statistiques</a>
                </li>

                {# --- Liens réservés à l'ADMIN --- #}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_absence_crud_index') }}">Gérer les absences</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-success btn-sm" href="{{ path('app_intern_crud_new') }}">
                            + Ajouter un stagiaire
                        </a>
                    </li>
                    <li class="nav-item">
                        <span class="navbar-text text-light me-2">
                            {{ app.user.userIdentifier }}
                        </span>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-light btn-sm" href="{{ path('app_logout') }}">
                            Déconnexion
                        </a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="btn btn-outline-light btn-sm" href="{{ path('app_login') }}">
                            Connexion
                        </a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>