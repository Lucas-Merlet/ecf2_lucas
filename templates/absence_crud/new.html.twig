{% extends 'base.html.twig' %}

{% block title %}Ajouter une absence{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h1 class="h4 mb-0">Ajouter une absence</h1>
                    </div>
                    <div class="card-body">
                        {{ include('absence_crud/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}