{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="mt-3 d-flex gap-2">
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('app_absence_crud_index') }}" class="btn btn-outline-secondary">Annuler</a>
    </div>
{{ form_end(form) }}