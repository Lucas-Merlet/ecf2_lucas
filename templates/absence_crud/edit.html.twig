{% extends 'base.html.twig' %}

{% block title %}Modifier une absence{% endblock %}

{% block body %}
    <div class="container my-4">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-header">
                        <h1 class="h4 mb-0">Modifier une absence</h1>
                    </div>
                    <div class="card-body">
                        {{ include('absence_crud/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}